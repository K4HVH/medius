//! Fire-and-go command methods (§3) — implemented in Task 3.3.

// (Intentionally empty until Task 3.3 — the module exists so `mod commands;` resolves and the file
// is ready for the command surface.)
